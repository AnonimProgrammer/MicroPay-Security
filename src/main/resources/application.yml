spring:
  application:
    name: micropay-security

  datasource:
    url: jdbc:postgresql://localhost:5450/security_db
    username: user
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  liquibase:
    enabled: false
    change-log: classpath:db/changelog/db.changelog-master.yaml

server:
  port: 8150

security:
  jwt:
    secret: ed289801ff08db5d4fd8d71cd29ff018bc333b3ab1b9c1d0a261257ed28ae9b3
    access-token-validity: 900000   # 15 minutes
    refresh-token-validity: 604800000 # 7 days
